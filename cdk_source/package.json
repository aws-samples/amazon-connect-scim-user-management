{
  "name": "cdk_source",
  "version": "0.1.0",
  "bin": {
    "cdk_source": "bin/cdk_source.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk"
  },
  "//": "Direct dependencies are pinned to exact versions. No package-lock.json is committed, so npm ci and npm audit are unavailable and the transitive graph resolves at install time. Bump deliberately, then re-run: npm install && npx tsc --noEmit && npx jest && npx cdk synth. For an audit: npm install --package-lock-only && npm audit.",
  "devDependencies": {
    "@types/jest": "30.0.0",
    "@types/node": "26.4.0",
    "aws-cdk": "2.1139.0",
    "cdk-nag": "3.0.2",
    "jest": "30.5.1",
    "ts-jest": "29.4.12",
    "ts-node": "10.9.2",
    "typescript": "5.9.3"
  },
  "dependencies": {
    "aws-cdk-lib": "2.267.0",
    "constructs": "10.8.1",
    "source-map-support": "0.5.21"
  }
}
