# Pinned toolchain for running the Lambda handler tests and linters, and for
# validating the CloudFormation template.
#
# These are development dependencies only. The Lambda functions themselves add no
# packages to the deployment bundle: boto3 and botocore are provided by the
# python3.14 Lambda runtime, and nothing else is imported at runtime.
#
# Install with:
#   python3.14 -m venv .venv
#   .venv/bin/pip install -r requirements-dev.txt
#
# Every pin below, including transitive ones, was produced by resolving the direct
# dependencies (boto3, botocore, pytest, ruff, cfn-lint) in a clean environment and
# freezing the result -- not written by hand. To upgrade, repeat that in a clean
# venv, re-run the test suite and the linters, and commit the new freeze.

PyYAML==6.0.3
Pygments==2.21.0
boto3==1.43.86
botocore==1.43.86
cfn-lint==1.56.0
iniconfig==2.3.0
jmespath==1.1.0
jsonpatch==1.33
jsonpointer==3.1.1
mpmath==1.3.0
networkx==3.6.1
packaging==26.3
pluggy==1.6.0
pytest==9.1.1
python-dateutil==2.9.0.post0
regex==2026.9.3
ruff==0.16.5
s3transfer==0.19.2
six==1.17.0
sympy==1.14.0
typing_extensions==4.16.0
urllib3==2.7.0
